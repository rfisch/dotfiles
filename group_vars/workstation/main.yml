---
# set all vaulted variables in subgroup
git_user: "{{ vault_git_user }}"
ssh:
  config: "{{ vault_ssh_config }}"
  public_keys: "{{ vault_ssh_public_keys }}"
  private_keys: "{{ vault_ssh_private_keys }}"
