---
cron_command: "{{ lookup('template', 'cron_command.sh.j2') | trim }}"
logfile: /var/log/dotfiles.log
repo_url: git://github.com/aglorei/dotfiles.git
schedule: '*/15 * * * *'
workdir: /tmp/dotfiles

homebrew:
  brews:
    - ansible
    - logrotate
pacman:
  packages:
    - ansible
    - cronie
